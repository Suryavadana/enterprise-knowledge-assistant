server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Try the exact static file, then a directory, then fall back to
        # index.html so client-side routing can take over.
        try_files $uri $uri/ /index.html;
    }
}
